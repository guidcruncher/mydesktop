{
  "name": "mydesktop",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "cfg": "nano ./server/config/desktop.yml",
    "install:all": "cd client && npm install && cd ../server && npm install",
    "server": "cd server && npm run dev-server",
    "client": "cd client && npm run dev-client",
    "format": "cd client && prettier ./src --write && cd ../server && prettier ./src --write",
    "dev": "npm run format && concurrently \"npm run server\" \"npm run client\"",
    "prod": "docker compose stop & docker compose rm -f && docker compose -f ./docker-compose.yml up --build -d",
    "build": "bash ./build.sh", 
    "publish":"docker buildx build --file ./Dockerfile --tag docker.io/guidcruncher/mydesktop:latest --progress=plain --push ."
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "3.7.4"
  }
}
