{
  "name": "mydesktop-client",
  "version": "0.0.0",
  "scripts": {
    "dev": "cd .. && npm run dev",
    "dev-client": "vite --host",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "sass": "sass ./src/styles/styles-full.scss",
    "lint": "eslint . --fix --cache",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.2",
    "typescript": "^5.0.2",
    "vite": "^7.2.7",
    "vue-tsc": "^3.1.8"
  }
}
